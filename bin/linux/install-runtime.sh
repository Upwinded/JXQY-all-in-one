# SDL3, SDL3_image, SDL3_ttf and SDL3_mixer are supplied by
# ThirdParty/devel/linux/SDL. Only the compiler and FFmpeg development
# packages are installed from the system package manager.
sudo apt-get install build-essential cmake libswscale-dev libavcodec-dev libavutil-dev libavdevice-dev libavfilter-dev libavformat-dev libswresample-dev
